<?xml version="1.0" encoding="UTF-8"?>

<!--
	Copyright (c) 2010-2025 Evolveum and contributors

	Licensed under the EUPL-1.2 or later.
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.evolveum.midpoint</groupId>
	<artifactId>integration-catalog</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>integration-catalog</name>
	<description>Integration catalog</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!-- Overridden to npx.cmd by the npx-windows profile -->
		<npx.executable>npx</npx.executable>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- Spring Boot 4 auto-configures Jackson 3, whose ObjectMapper lives in
		     tools.jackson.databind. This module restores the auto-configured
		     com.fasterxml.jackson.databind.ObjectMapper bean that BundleService
		     injects. Kept on Jackson 2 deliberately: ConnectorMapper serialises a
		     DTO and signs the resulting bytes, so a serialiser swap could change
		     signatures for connectors that were already signed. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-jackson2</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-logging</artifactId>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Boot 4 moved the @WebMvcTest slice out of spring-boot-starter-test
             into its own module; ControllerTest needs it. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.46</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.1.0</version>
        </dependency>

        <dependency>
            <groupId>org.gitlab4j</groupId>
            <artifactId>gitlab4j-api</artifactId>
            <version>6.3.0</version>
        </dependency>

        <dependency>
            <groupId>org.kohsuke</groupId>
            <artifactId>github-api</artifactId>
            <version>2.0-rc.7</version>
        </dependency>

        <!-- Parses the Markdown tutorial text so it can be converted to AsciiDoc for the download bundle -->
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark</artifactId>
            <version>0.29.0</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<!--
                 This plugin is used to parse current project version for later use with npm version which requires semantic versioning
                 -->
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>3.4.0</version>
				<executions>
					<execution>
						<id>parse-version</id>
						<goals>
							<goal>parse-version</goal>
						</goals>
						<configuration>
							<propertyPrefix>parsedVersion</propertyPrefix>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Software Bill of Materials generation. Opt-in, so ordinary builds are unaffected:

		         mvn package -Psbom

		     produces two CycloneDX documents under target/:
		         backend-sbom.json   - Maven dependency tree (test scope excluded)
		         frontend-sbom.json  - npm dependency tree of angular-frontend (dev deps excluded)

		     Both are standard CycloneDX JSON and can be fed to Dependency-Track, Grype,
		     Trivy or any other scanner for vulnerability and license checks.

		     The frontend part requires Node.js on the PATH. It resolves from angular-frontend's
		     package-lock.json alone, so no prior "npm install" is needed. -->
		<profile>
			<id>sbom</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.cyclonedx</groupId>
						<artifactId>cyclonedx-maven-plugin</artifactId>
						<version>2.9.1</version>
						<executions>
							<!-- Spring Boot's parent manages this plugin and contributes an execution
                                 of its own (id "default") that writes an SBOM into the jar at
                                 META-INF/sbom/application.cdx.json. Unbinding it keeps the BOM being
                                 built once, at the single documented location below. -->
							<execution>
								<id>default</id>
								<phase>none</phase>
							</execution>
							<execution>
								<id>backend-sbom</id>
								<phase>package</phase>
								<goals>
									<goal>makeBom</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<projectType>application</projectType>
							<outputFormat>json</outputFormat>
							<outputName>backend-sbom</outputName>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<includeBomSerialNumber>true</includeBomSerialNumber>
							<includeTestScope>false</includeTestScope>
						</configuration>
					</plugin>
					<plugin>
						<groupId>com.github.eirslett</groupId>
						<artifactId>frontend-maven-plugin</artifactId>
						<version>1.15.4</version>

						<configuration>
							<nodeDownloadRoot>
								https://nexus.evolveum.com/nexus/repository/nodejs-dist/
							</nodeDownloadRoot>
							<npmDownloadRoot>
								https://nexus.evolveum.com/nexus/repository/npmjs-dist/
							</npmDownloadRoot>

							<nodeVersion>v22.18.0</nodeVersion>
							<npmVersion>10.9.3</npmVersion>
							<installDirectory>${project.build.directory}</installDirectory>
						</configuration>

						<executions>

							<execution>
								<id>install-node-and-npm</id>
								<goals>
									<goal>install-node-and-npm</goal>
								</goals>
							</execution>

							<execution>
								<id>frontend-sbom</id>
								<phase>package</phase>
								<goals>
									<goal>npx</goal>
								</goals>
								<configuration>
									<arguments>
										--yes
										@cyclonedx/cyclonedx-npm
										--package-lock-only
										--omit
										dev
										--ignore-npm-errors
										--output-format
										JSON
										--output-file
										${project.build.directory}/frontend-sbom.json
									</arguments>
									<workingDirectory>
										${project.basedir}/angular-frontend
									</workingDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>dependencytrack</id>
			<build>
				<plugins>
					<plugin>
						<groupId>io.github.pmckeown</groupId>
						<artifactId>dependency-track-maven-plugin</artifactId>
						<version>1.10.2</version>
						<configuration>
							<dependencyTrackBaseUrl>${env.DTRACK_URL}</dependencyTrackBaseUrl>
							<apiKey>${env.DTRACK_TOKEN}</apiKey>
							<parentName>integration-catalog</parentName>
							<parentVersion>${parsedVersion.majorVersion}.${parsedVersion.minorVersion}</parentVersion>
							<pollingConfig>
								<enabled>true</enabled>
								<pause>500</pause>
								<attempts>100</attempts>
								<timeUnit>MILLIS</timeUnit>
							</pollingConfig>
						</configuration>
						<executions>
							<execution>
								<id>upload-backend-bom</id>
								<phase>verify</phase>
								<goals>
									<goal>upload-bom</goal>
								</goals>
								<configuration>
									<projectName>backend</projectName>
									<bomLocation>./target/backend-sbom.json</bomLocation>
								</configuration>
							</execution>

							<execution>
								<id>upload-frontend-bom</id>
								<phase>verify</phase>
								<goals>
									<goal>upload-bom</goal>
								</goals>
								<configuration>
									<projectName>frontend</projectName>
									<bomLocation>./target/frontend-sbom.json</bomLocation>
								</configuration>
							</execution>

						</executions>
					</plugin>

				</plugins>
			</build>
		</profile>

		<!-- npx is a .cmd shim on Windows and must be invoked under that name. -->
		<profile>
			<id>npx-windows</id>
			<activation>
				<os>
					<family>windows</family>
				</os>
			</activation>
			<properties>
				<npx.executable>npx.cmd</npx.executable>
			</properties>
		</profile>
	</profiles>

</project>
